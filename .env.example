APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
FRONTEND_URL=http://localhost:3000

# شماره‌ای که سوپرادمین با آن فقط وارد پنل مدیریت می‌شود.
SUPER_ADMIN_MOBILE=09120000000
SUPER_ADMIN_PASSWORD=change-this-secure-password
SUPER_ADMIN_NAME="Super Admin"

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-account@gmail.com
MAIL_PASSWORD=your-16-character-google-app-password
MAIL_FROM_ADDRESS="your-account@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# --------------------------------------------------------------------
# درگاه پرداخت زیبال
# --------------------------------------------------------------------
# برای تست: مقدار "zibal" به‌عنوان مرچنت، تراکنش‌های آزمایشی
# (بدون پرداخت واقعی) می‌سازد. وقتی مرچنت واقعی از زیبال گرفتی،
# فقط همین یک خط را در .env واقعی سرور عوض کن — هیچ کد دیگری
# نیاز به تغییر ندارد.
ZIBAL_BASE_URL=https://gateway.zibal.ir/v1
ZIBAL_MERCHANT=zibal
ZIBAL_CALLBACK_URL="${APP_URL}/api/v1/payment/callback"

# کاوه‌نگار — بعد از خرید پلن پیشرفته، این سه‌تا را پر کن و توی
# AppServiceProvider، binding مربوط به SmsProviderInterface را از
# MockSmsProvider به KavenegarProvider تغییر بده.
KAVENEGAR_API_KEY=
KAVENEGAR_SENDER=
KAVENEGAR_OTP_TEMPLATE=

# تنظیمات نسخه عملیاتی: در سرور واقعی APP_ENV=production،
# APP_DEBUG=false و LOG_LEVEL=warning باشد.

# پشتیبان‌گیری روزانه با scheduler انجام می‌شود. اجرای دستی:
# php artisan app:backup --keep=14
